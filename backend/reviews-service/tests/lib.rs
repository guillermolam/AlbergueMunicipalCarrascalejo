// Tests disabled: These relied on the Spin SDK HTTP types which have been removed.
// The reviews-service now uses Cloudflare Workers (worker crate).
// Re-enable when proper integration test infrastructure is in place.
//
// To test the service, use `wrangler dev` and curl/httpie against the local dev server.
