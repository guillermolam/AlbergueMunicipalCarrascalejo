[package]
name = "mqtt-broker"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.134"
# Cloudflare Workers SDK
worker = "0.7.5"
chrono = { version = "0.4.43", features = ["serde"] }
