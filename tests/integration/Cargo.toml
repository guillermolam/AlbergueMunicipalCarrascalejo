[package]
name = "integration-tests"
version = "0.1.0"
edition = "2021"

# Explicit lib target: mod.rs is the crate root.
# Without this Cargo finds no targets and cargo fmt/clippy fail.
[lib]
path = "mod.rs"

[dependencies]
# Core dependencies
anyhow = "1.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
uuid = { version = "1.0", features = ["v4", "serde"] }
chrono = { version = "0.4", features = ["serde"] }

# Async runtime
tokio = { version = "1.0", features = [
  "sync",
  "macros",
  "io-util",
  "rt",
  "time",
  "test-util",
] }
futures = "0.3"

# HTTP
http = "1.1"
reqwest = { version = "0.11", features = ["json"] }

# Testing
tokio-test = "0.4"
wiremock = "0.5"

# Testing assertions
speculoos = "0.11"
