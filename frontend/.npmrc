# pnpm-specific settings moved to package.json "pnpm" field
# to avoid npm warnings when wrangler runs internally.
# shamefully-hoist=false is pnpm's default (no need to set it).
# onlyBuiltDependencies is in package.json > pnpm.onlyBuiltDependencies.
